Negocie Fácil - protótipo
Frontend: React + Vite + Tailwind
Backend: FastAPI (mock endpoints)
To run frontend: cd frontend && npm install && npm run dev
To run backend: cd backend && pip install -r requirements.txt && uvicorn main:app --reload --port 8000
